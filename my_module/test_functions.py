"""Test for my functions.

Note: because these are 'empty' functions (return None), here we just test
  that the functions execute, and return None, as expected.
"""

from functions import start_quiz, continue_on, time_is_up, start_bonus
##
##

def test_start_quiz():

    assert start_quiz() == None
    assert callable(start_quiz)
    assert isinstance(start_quiz
    
"""def test_continue_on():
    
    assert continue_on() == None
    assert callable(continue_on)
    assert isinstance(continue_on('start'), str)
    assert isinstance(continue_on('stop'), str) """

def test_time_is_up():

    assert time_is_up() == None
    assert callable(time_is_up)
    assert isinstance(
        
    
def test_start_bonus():
    
    assert start_bonus() == None
    assert callable(start_bonus)
    assert isinstance(



                 
    